1- selectionner le fichier, git bash here
2- git init
3- git status
4- git add
5- git status
6- git commit --message "nomDuCommit"


git diff // pour voir ce qui a été modifié dans un fichier
git log  // va lister tout les commit depuis le debut de projet

git branch nomDeMaBranche  // va creer une branche
git branch  //va afficher toutes les branches existantes
git checkout nomDeMaBranche  //pour acceder a une branche specifique
git commit / add / status // marche idem dans la branch
git checkout master  //pour revenir a la branche principale


//quand on est dans une branch pour tout rapatrier sur le master
git checkout master //pour rapatrier les change d 1 branche sur master
git diff master..nomDeMaBranche //pour rapatrier les change d 1 branche sur master
git merge nomDeMaBranche   //pour rapatrier les change d 1 branche sur master 

//pur exclure des fichiers d un commit, genre fichier "sesible"
git ignore